@/controllers/auth/nex controllers/nex_auth_contollers_controller.js  we want to create crud operations here, similarly to @/controllers/auth/nex super users/nex_auth_supers_controller.js  but for controllers. our controllers table is named  controllers_nex_lock it has id bigint as pk and name as varchar(255), email varchar(255) role is by default 'controller' and is unchangeable and company_id is the id of a row in customers_nex_lock and it has to be a valid one  and we have a column boolean called status which by default is true. similarly to @/controllers/auth/nex super users/nex_auth_supers_controller.js  we must also have a password field, which automatically creates a user even for the controller at users_nex_lock. company_id in that table also tells us for which company they work for